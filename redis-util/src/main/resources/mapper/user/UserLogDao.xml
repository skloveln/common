<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zpc.mvc.user.log.UserLogDao" >

    <insert id="insert">
        INSERT INTO lb_user_log (
            id,
            `key`,
            text,
            create_date
        ) VALUES (
            #{id},
            #{key},
            #{text},
            #{createDate}
        )
    </insert>

</mapper>